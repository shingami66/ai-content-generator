# شرح مشروع توليد المحتوى بالذكاء الاصطناعي
## بالعربي - اللهجة السودانية

---

## 📋 نظرة عامة على المشروع

المشروع ده عبارة عن موقع ويب بيستخدم الذكاء الاصطناعي لتوليد صور وفيديوهات. الموقع مكون من **ثلاثة أجزاء رئيسية**:

1. **الفرونت إند (Frontend)** - الواجهة اللي المستخدم بيشوفها
2. **الباك إند (Backend)** - الخادم اللي بيستقبل الطلبات وبيعالجها
3. **قاعدة البيانات (Database)** - المكان اللي بتتخزن فيه البيانات

---

## 🏗️ البنية المعمارية (Architecture)

### 1. الفرونت إند (Frontend)
**الموقع:** `src/frontend/`
**التقنيات المستخدمة:**
- React + TypeScript
- Vite (أداة البناء)
- React Router (للتنقل بين الصفحات)

**الملفات المهمة:**
- `App.tsx` - الملف الرئيسي اللي بيحدد الصفحات والروابط
- `DashboardPage.tsx` - صفحة توليد المحتوى
- `api/api.ts` - ملف الاتصال بالباك إند
- `context/AppContext.tsx` - إدارة الحالة العامة للتطبيق

**كيف بيشتغل:**
- المستخدم بيفتح الموقع في المتصفح
- React بيعرض الصفحات والواجهات
- لما المستخدم يعمل أي إجراء (مثلاً: يضغط زر توليد صورة)، الكود بيعمل طلب HTTP للباك إند

---

### 2. الباك إند (Backend)
**الموقع:** `src/backend/src/`
**التقنيات المستخدمة:**
- Node.js + Express.js
- MySQL (قاعدة البيانات)
- OpenAI API (لتوليد الصور)
- Google Gemini (لتوليد النصوص - Endpoint منفصل)

**البنية:**
```
backend/src/
├── server.js          # نقطة البداية - بيشغل السيرفر
├── config/
│   └── database.js    # إعدادات الاتصال بقاعدة البيانات
├── routes/            # تعريف المسارات (Routes)
│   ├── auth.js        # مسارات تسجيل الدخول والتسجيل
│   ├── content.js    # مسارات توليد المحتوى
│   ├── users.js      # مسارات المستخدمين
│   └── ...
├── controllers/       # المنطق التجاري (Business Logic)
│   ├── contentController.js  # منطق توليد المحتوى
│   └── ...
└── middleware/       # طبقات الحماية والتحقق
    ├── auth.js       # التحقق من التوكن
    └── validation.js # التحقق من صحة البيانات
```

---

### 3. قاعدة البيانات (Database)
**النوع:** MySQL
**الجداول الرئيسية:**
- `registereduser` - بيانات المستخدمين
- `content` - المحتوى المولد (صور وفيديوهات)
- `subscription` - اشتراكات المستخدمين

---

## 🔄 كيف بيتم الاتصال بين الأجزاء؟

### الخطوة 1: المستخدم يعمل إجراء في الفرونت إند

**مثال: توليد صورة**

1. المستخدم بيكتب وصف للصورة في `DashboardPage.tsx`
2. المستخدم بيدوس على زر "Generate"
3. الكود بيعمل استدعاء للدالة `handleGenerate('image')`

```typescript
// في DashboardPage.tsx
const handleGenerate = async (type: 'image' | 'video') => {
  // 1. التحقق من صحة البيانات
  // 2. التحقق من الحد اليومي
  // 3. استدعاء API
  const response = await contentAPI.generateContent(user.id, type, desc);
}
```

---

### الخطوة 2: الفرونت إند بيعمل طلب HTTP للباك إند

**الملف:** `src/frontend/src/api/api.ts`

```typescript
// دالة عامة للاتصال بالباك إند
const apiCall = async (endpoint: string, options: RequestInit = {}) => {
  const fullUrl = `http://localhost:3001/api${endpoint}`;
  
  // إضافة التوكن في الهيدر
  const token = localStorage.getItem('token');
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  }
  
  // إرسال الطلب
  const response = await fetch(fullUrl, options);
  return response.json();
}

// استدعاء توليد المحتوى
contentAPI.generateContent = async (userId, type, description) => {
  return apiCall('/content/generate', {
    method: 'POST',
    body: JSON.stringify({ userId, type, description })
  });
}
```

**ما يحدث:**
- الطلب بيوصل للباك إند على العنوان: `http://localhost:3001/api/content/generate`
- الطلب محمول فيه: `userId`, `type`, `description`
- في الهيدر: التوكن (Token) للتحقق من الهوية

---

### الخطوة 3: الباك إند بيستقبل الطلب

**الملف:** `src/backend/src/server.js`

```javascript
// تسجيل المسارات
app.use('/api/content', contentRoutes);
```

**الملف:** `src/backend/src/routes/content.js`

```javascript
// تعريف المسار
router.post('/generate', 
  validateContentGeneration,  // 1. التحقق من البيانات
  authenticateToken,           // 2. التحقق من التوكن
  ContentController.generateContent  // 3. تنفيذ المنطق
);
```

**ما يحدث بالترتيب:**

1. **Middleware: Validation** - التحقق من صحة البيانات
   - التأكد إن `userId`, `type`, `description` موجودين وصحيحين

2. **Middleware: Authentication** - التحقق من التوكن
   - فك تشفير التوكن
   - التأكد إن المستخدم مسجل دخول
   - إضافة معلومات المستخدم للطلب (`req.user`)

3. **Controller** - تنفيذ المنطق التجاري

---

### الخطوة 4: Controller بيعالج الطلب

**الملف:** `src/backend/src/controllers/contentController.js`

```javascript
static async generateContent(req, res) {
  const { userId, type, description } = req.body;
  
  if (type === 'image') {
    // 1. استدعاء OpenAI API
    const response = await openai.images.generate({
      model: 'dall-e-3',
      prompt: description,
      size: '1024x1024'
    });
    
    // 2. تحميل الصورة وحفظها محلياً
    const imageUrl = response.data[0].url;
    const localUrl = await downloadToUploads(imageUrl, 'png', req);
    
    // 3. حفظ في قاعدة البيانات
    await db.query(
      'INSERT INTO content (Title, OwnerID, ContentType, Description, URL) VALUES (?, ?, ?, ?, ?)',
      [title, userId, type, description, localUrl]
    );
    
    // 4. إرجاع النتيجة للفرونت إند
    res.json({
      success: true,
      contentId: result.insertId,
      url: localUrl
    });
  }
}
```

**ما يحدث:**

1. **استدعاء API خارجي** (OpenAI)
   - الباك إند بيعمل طلب للـ API الخارجي
   - الـ API بيرجع رابط الصورة/الفيديو

2. **تحميل الملف محلياً**
   - الباك إند بتحمل الصورة/الفيديو من الرابط الخارجي
   - بتحفظه في مجلد `uploads/`
   - بيرجع رابط محلي: `http://localhost:3001/uploads/filename.png`

3. **حفظ في قاعدة البيانات**
   - إدراج سجل جديد في جدول `content`
   - حفظ: `userId`, `type`, `description`, `url`

4. **إرجاع النتيجة**
   - الباك إند بيرجع JSON للفرونت إند
   - يحتوي على: `success`, `contentId`, `url`

---

### الخطوة 5: الاتصال بقاعدة البيانات

**الملف:** `src/backend/src/config/database.js`

```javascript
// إنشاء Pool للاتصالات
const pool = mysql.createPool({
  host: process.env.DB_HOST,
  user: process.env.DB_USER,
  password: process.env.DB_PASSWORD,
  database: process.env.DB_NAME,
  connectionLimit: 10  // عدد الاتصالات المتزامنة
});

// استخدام الـ Pool
const [result] = await pool.query(
  'INSERT INTO content (...) VALUES (...)',
  [values]
);
```

**ما يحدث:**
- الباك إند بيستخدم `mysql2` للاتصال بـ MySQL
- الـ Pool بيسمح بعدة اتصالات متزامنة
- كل استعلام SQL بيتم عبر الـ Pool

---

### الخطوة 6: الفرونت إند بيستقبل النتيجة

**الملف:** `src/frontend/src/pages/DashboardPage.tsx`

```typescript
const response = await contentAPI.generateContent(user.id, type, desc);

// إضافة المحتوى الجديد للقائمة
const newContent = {
  id: response.contentId,
  type: type,
  description: desc,
  url: response.url  // الرابط المحلي
};

setGeneratedContent((prev) => [newContent, ...prev]);
```

**ما يحدث:**
- الفرونت إند بيستقبل الـ JSON من الباك إند
- بيضيف المحتوى الجديد للقائمة
- بيستخدم الرابط المحلي لعرض الصورة/الفيديو

---

## 🔐 نظام المصادقة (Authentication)

### كيف بيشتغل:

1. **التسجيل/تسجيل الدخول:**
   ```javascript
   // الفرونت إند
   const response = await authAPI.login(email, password);
   localStorage.setItem('token', response.token);
   ```

2. **إرسال التوكن مع كل طلب:**
   ```typescript
   // في api.ts
   headers: {
     'Authorization': `Bearer ${token}`
   }
   ```

3. **الباك إند بيفحص التوكن:**
   ```javascript
   // في middleware/auth.js
   const token = req.headers.authorization?.split(' ')[1];
   const decoded = jwt.verify(token, process.env.JWT_SECRET);
   req.user = decoded;  // إضافة معلومات المستخدم للطلب
   ```

---

## 📊 نظام تتبع التوليد (Generation Tracking)

### كيف بيشتغل:

1. **التحقق من الحد اليومي:**
   ```javascript
   // في routes/generations.js
   router.get('/can-generate/:userId', async (req, res) => {
     // عد السجلات اللي اتعملت اليوم
     const [count] = await db.query(
       `SELECT COUNT(*) as count 
        FROM content 
        WHERE OwnerID = ? AND DATE(DateCreated) = CURDATE()`,
       [userId]
     );
     
     const limit = user.subscriptionType === 'premium' ? 999999 : 5;
     res.json({ canGenerate: count[0].count < limit });
   });
   ```

2. **زيادة العداد بعد التوليد:**
   ```javascript
   // بعد نجاح التوليد
   await db.query(
     'INSERT INTO content (...) VALUES (...)',
     [values]
   );
   // السجل الجديد بيزود العداد تلقائياً
   ```

3. **إعادة تعيين يومي:**
   ```javascript
   // في utils/cronJobs.js
   cron.schedule('0 0 * * *', () => {
     // كل يوم في منتصف الليل
     // العداد بيترجع تلقائياً لأن الاستعلام بيستخدم CURDATE()
   });
   ```

---

## 🎬 مثال كامل: توليد فيديو

### 1. المستخدم في الفرونت إند:
```
المستخدم بيكتب: "A cinematic superhero race"
بيدوس على: "Generate Video"
```

### 2. الفرونت إند → الباك إند:
```http
POST http://localhost:3001/api/content/generate
Headers: Authorization: Bearer <token>
Body: {
  "userId": 10,
  "type": "video",
  "description": "A cinematic superhero race..."
}
```

### 3. الباك إند → مزود الفيديو:
```javascript
// في contentController.js
// ملاحظة: تم إزالة Runway من المشروع. لتفعيل توليد الفيديو عبر Google ستحتاج إعداد مزود (مثل Vertex AI Veo) ثم ربطه في الباك إند.
```

### 4. الباك إند → Polling للنتيجة:
```javascript
// الانتظار حتى يكتمل التوليد
while (!videoUrl) {
  await sleep(5000);  // انتظار 5 ثواني
  const status = await checkTaskStatus(taskId);
  if (status === 'completed') {
    videoUrl = statusResponse.data.output;
    break;
  }
}
```

### 5. الباك إند → تحميل الفيديو محلياً:
```javascript
// تحميل الفيديو من Runway
const videoResponse = await axios({
  method: 'GET',
  url: videoUrl,
  responseType: 'stream'
});

// حفظ في uploads/
const localUrl = await downloadToUploads(videoUrl, 'mp4', req);
// النتيجة: http://localhost:3001/uploads/video_123.mp4
```

### 6. الباك إند → قاعدة البيانات:
```sql
INSERT INTO content (Title, OwnerID, ContentType, Description, URL, DateCreated)
VALUES ('A cinematic...', 10, 'video', 'A cinematic...', 'http://localhost:3001/uploads/video_123.mp4', NOW())
```

### 7. الباك إند → الفرونت إند:
```json
{
  "success": true,
  "contentId": 44,
  "url": "http://localhost:3001/uploads/video_123.mp4",
  "description": "A cinematic superhero race..."
}
```

### 8. الفرونت إند يعرض النتيجة:
```tsx
<video src="http://localhost:3001/uploads/video_123.mp4" controls />
```

---

## 🛡️ الحماية والأمان

### 1. Rate Limiting (تحديد معدل الطلبات):
```javascript
// في server.js
const generationLimiter = rateLimit({
  windowMs: 60 * 1000,  // دقيقة واحدة
  max: 10  // 10 طلبات فقط
});
app.use('/api/content/generate', generationLimiter);
```

### 2. Authentication Middleware:
```javascript
// كل طلب محتاج توكن صحيح
router.post('/generate', authenticateToken, ...);
```

### 3. Validation:
```javascript
// التحقق من صحة البيانات قبل المعالجة
router.post('/generate', validateContentGeneration, ...);
```

---

## 📁 هيكل الملفات المهمة

```
ai-app/
├── src/
│   ├── frontend/              # الواجهة الأمامية
│   │   ├── src/
│   │   │   ├── pages/        # الصفحات
│   │   │   │   └── DashboardPage.tsx
│   │   │   ├── api/          # الاتصال بالباك إند
│   │   │   │   └── api.ts
│   │   │   └── context/      # إدارة الحالة
│   │   │       └── AppContext.tsx
│   │   └── package.json
│   │
│   └── backend/              # الخادم
│       └── src/
│           ├── server.js      # نقطة البداية
│           ├── config/
│           │   └── database.js
│           ├── routes/        # المسارات
│           │   ├── content.js
│           │   └── auth.js
│           ├── controllers/   # المنطق التجاري
│           │   └── contentController.js
│           └── middleware/    # الحماية
│               └── auth.js
```

---

## 🎯 نقاط مهمة للشرح في المناقشة

### 1. **البنية المعمارية:**
- "المشروع مكون من ثلاث طبقات: الفرونت إند (React)، الباك إند (Node.js/Express)، وقاعدة البيانات (MySQL)"
- "استخدمت RESTful API للاتصال بين الفرونت إند والباك إند"

### 2. **نظام المصادقة:**
- "استخدمت JWT (JSON Web Tokens) للمصادقة"
- "كل طلب محتاج توكن صحيح في الهيدر"
- "التوكن بيتم فحصه في Middleware قبل الوصول للـ Controller"

### 3. **توليد المحتوى:**
- "للصور: استخدمت OpenAI DALL-E 3 API"
- "للفيديوهات: استخدمت Runway Veo 3 API"
- "بعد استلام الملف من الـ API الخارجي، بتحمله محلياً في مجلد uploads لتجنب مشاكل CORS والانقطاع"

### 4. **تتبع التوليد:**
- "نظام تتبع يومي: المستخدم المجاني عنده 5 توليدات في اليوم"
- "استخدمت استعلام SQL بـ CURDATE() للعد اليومي"
- "Cron Job بيشغل إعادة تعيين يومي (لكن فعلياً الاستعلام بيشتغل تلقائياً)"

### 5. **الأمان:**
- "Rate Limiting: 10 طلبات توليد في الدقيقة"
- "Validation Middleware: التحقق من صحة البيانات"
- "Authentication Middleware: التحقق من التوكن"

### 6. **معالجة الأخطاء:**
- "Error Handling Middleware في الباك إند"
- "Try-Catch في كل العمليات الحرجة"
- "رسائل خطأ واضحة للمستخدم"

---

## 💡 نصائح للشرح

1. **ابدأ بالصورة الكبيرة:** اشرح البنية العامة قبل التفاصيل
2. **استخدم أمثلة عملية:** "مثلاً لما المستخدم يضغط زر توليد صورة..."
3. **اشرح التدفق (Flow):** من الفرونت إند → الباك إند → قاعدة البيانات
4. **اذكر التقنيات:** React, Node.js, Express, MySQL, OpenAI, Runway
5. **ركز على الأمان:** Authentication, Rate Limiting, Validation

---

## ❓ أسئلة متوقعة وإجاباتها

**س: ليه استخدمت React للفرونت إند؟**
- "React مكتبة قوية ومرنة، وبتسمح بإدارة الحالة بسهولة عبر Context API"

**س: كيف بتتعامل مع الطلبات الطويلة (مثل توليد الفيديو)؟**
- "استخدمت Polling: الباك إند بيعمل طلبات دورية للـ Runway API كل 5 ثواني حتى يكتمل التوليد"

**س: كيف بتحمي الموقع من الهجمات؟**
- "Rate Limiting للحد من الطلبات، Authentication للتحقق من الهوية، Validation للتحقق من البيانات"

**س: كيف بتتعامل مع قاعدة البيانات؟**
- "استخدمت Connection Pooling للتحكم في عدد الاتصالات المتزامنة وتحسين الأداء"

---

**تم إعداد هذا الشرح بواسطة AI Assistant**
**تاريخ:** 2024
