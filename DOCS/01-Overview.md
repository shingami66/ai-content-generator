# 📱 نظرة عامة على المشروع

## 🎯 ما هو هذا المشروع؟

**AI Content Generator** هو منصة ويب تسمح للمستخدمين بتوليد **صور وفيديوهات** باستخدام الذكاء الاصطناعي من خلال كتابة وصف نصي فقط!

### مثال بسيط:
```
المستخدم يكتب: "A beautiful sunset over mountains"
       ↓
النظام يولد: 🖼️ صورة جميلة لغروب الشمس فوق الجبال
```

---

## ✨ المميزات الرئيسية

### 1️⃣ **للمستخدمين المجانيين (Free Users):**
- ✅ تسجيل حساب مجاني
- ✅ 5 توليدات في اليوم
- ✅ حفظ المحتوى المولد
- ✅ معرض لعرض التوليدات
- ✅ إرسال تغذية راجعة

### 2️⃣ **للمستخدمين المميزين (Premium Users):**
- ✅ **توليدات غير محدودة** ♾️
- ✅ جودة أعلى
- ✅ أولوية في المعالجة
- ✅ دعم فني 24/7

### 3️⃣ **الميزات التقنية:**
- ✅ واجهة مستخدم جميلة (UI/UX)
- ✅ تصميم متجاوب (Responsive)
- ✅ حماية قوية (Authentication)
- ✅ قاعدة بيانات منظمة
- ✅ نظام دفع تجريبي (Stripe Test Mode)

---

## 🛠️ التقنيات المستخدمة (Tech Stack)

### Frontend (الواجهة الأمامية):
```
React 18          → مكتبة بناء الواجهات
TypeScript        → لغة برمجة (JavaScript + Types)
Vite              → أداة البناء (Build Tool)
React Router v6   → التنقل بين الصفحات
Context API       → إدارة الحالة العامة
CSS Modules       → التصميم
```

### Backend (الخادم):
```
Node.js           → بيئة التشغيل
Express.js        → Framework للـ APIs
MySQL (MariaDB)   → قاعدة البيانات
mysql2            → مكتبة الاتصال بالقاعدة
node-cron         → جدولة المهام التلقائية
Stripe            → بوابة الدفع (Test Mode)
```

### قاعدة البيانات:
```
MySQL Database: ai_db

جداول:
- registereduser     → المستخدمون
- content            → المحتوى المولد
- subscription       → الاشتراكات
- payment            → الدفعات
- complaintssuggestions → التغذية الراجعة
- administrator      → المدراء
```

---

## 🌐 كيف يعمل النظام؟ (بكلمات بسيطة)

### الخطوة 1: المستخدم يفتح الموقع
```
http://localhost:5173
```
- يرى صفحة ترحيبية (Landing Page)
- يضغط على "Login" أو "Sign Up"

### الخطوة 2: يسجل الدخول
```
Frontend (React) → يرسل Email + Password
       ↓
Backend (Express) → يتحقق من قاعدة البيانات
       ↓
Database (MySQL) → يبحث في جدول registereduser
       ↓
إذا صحيح → يعيد بيانات المستخدم
إذا خطأ → يعيد رسالة خطأ
```

### الخطوة 3: يذهب للـ Dashboard
```
Dashboard Page:
- يرى عداد: "Generations Today: 0/5"
- يكتب وصف: "A futuristic city at night"
- يضغط "Generate"
```

### الخطوة 4: التحقق من الحد
```
Frontend → يسأل Backend: هل يمكنه التوليد؟
       ↓
Backend → يتحقق من قاعدة البيانات:
       ↓
كم مرة ولد اليوم؟
       ↓
إذا أقل من 5 → ✅ يسمح
إذا 5 أو أكثر → ❌ يمنع ويعرض modal للترقية
```

### الخطوة 5: التوليد والحفظ
```
Frontend → يولد المحتوى (محاكاة - placeholder)
       ↓
يحفظ في Database → جدول content
       ↓
يزيد العداد → 1/5, 2/5, 3/5...
```

### الخطوة 6: الوصول للحد
```
عند 5/5:
- يظهر modal: "⚠️ Daily Limit Reached!"
- يعرض مميزات Premium
- زر "Upgrade to Premium"
```

### الخطوة 7: الترقية لـ Premium
```
المستخدم يضغط Upgrade
       ↓
يذهب لصفحة Subscription
       ↓
يختار طريقة دفع (Test Mode)
       ↓
Backend يحدث جدول subscription
       ↓
المستخدم يصبح Premium → توليدات غير محدودة!
```

---

## 🔑 المفاهيم الأساسية

### 1. Frontend vs Backend
```
Frontend (الواجهة)     Backend (الخادم)
├─ ما يراه المستخدم   ├─ المنطق والمعالجة
├─ React Components   ├─ APIs و Routes
├─ CSS التصميم        ├─ الاتصال بقاعدة البيانات
└─ التفاعل مع UI      └─ التحقق والأمان
```

### 2. API (Application Programming Interface)
```
طريقة تواصل Frontend مع Backend

مثال:
Frontend يقول: "أعطني معلومات المستخدم رقم 1"
Backend يرد: { id: 1, username: "mozfer", email: "..." }
```

### 3. Database (قاعدة البيانات)
```
مكان تخزين جميع البيانات:
- معلومات المستخدمين
- المحتوى المولد
- الاشتراكات
- الدفعات
```

### 4. REST API
```
نوع من APIs يستخدم HTTP methods:

GET    → قراءة البيانات
POST   → إضافة بيانات جديدة
PUT    → تحديث بيانات موجودة
DELETE → حذف بيانات
```

---

## 📊 معلومات سريعة للمناقشة

### الأسئلة المتوقعة:

**س: لماذا اخترت React؟**
✅ **الجواب:** 
- شائع وله مجتمع كبير
- Component-based (سهل إعادة الاستخدام)
- Virtual DOM (أداء سريع)
- مكتبات كثيرة متوفرة

**س: لماذا TypeScript وليس JavaScript؟**
✅ **الجواب:**
- يكتشف الأخطاء قبل التشغيل
- أسهل في الصيانة
- تلميحات أفضل في المحرر (IDE)

**س: كيف تحمي الحد اليومي من التلاعب؟**
✅ **الجواب:**
- الحد محفوظ في Backend (ليس في Frontend)
- كل طلب يتحقق من قاعدة البيانات
- العداد في Frontend للعرض فقط

**س: كيف يعمل نظام Premium؟**
✅ **الجواب:**
- جدول subscription في Database
- يحفظ تاريخ البداية والنهاية
- Backend يتحقق: هل EndDate > اليوم؟
- إذا نعم → Premium، إذا لا → Free

---

## 🎯 الملخص السريع

| المكون | الوظيفة |
|--------|---------|
| Frontend | الواجهة التي يراها المستخدم |
| Backend | المنطق والتحقق والأمان |
| Database | تخزين البيانات |
| API | التواصل بين Frontend و Backend |
| React | بناء واجهة المستخدم |
| Express | بناء Backend APIs |
| MySQL | قاعدة البيانات |

---

## ⏭️ التالي

الآن بعد أن فهمت النظرة العامة، انتقل إلى:
**[02-Project-Structure.md](./02-Project-Structure.md)** لفهم بنية المشروع بالتفصيل! 🚀
